import {
    IonToolbar,
    IonHeader,
    IonButtons,
    IonMenuButton,
    IonLabel,
    IonSegment,
    IonSegmentButton,
    IonTitle,
    IonButton,
} from '@ionic/react';


const Toolbar_002: React.FC = () => {
    return (
        <IonToolbar >
            <IonTitle>Login</IonTitle>
        </IonToolbar>
    );
};

export default Toolbar_002;